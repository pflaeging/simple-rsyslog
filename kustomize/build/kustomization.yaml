apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- buildconfig.yaml
- imagestream.yaml

secretGenerator:
- name: bitbucket-pull-ssh
  files:
  - ssh-privatekey=getter-key
  type: kubernetes.io/ssh-auth

generatorOptions:
  disableNameSuffixHash: true